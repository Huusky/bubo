const Discord = require('discord.js');

module.exports = {
    name: 'user-info',
    description: 'Get information about a user',
    usage: '<user name>',
    args: true,
    enabled: true,
    async execute(message, args) {
    }
}